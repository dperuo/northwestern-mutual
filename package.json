{
  "devDependencies": {
    "html-loader": "^0.4.3",
    "json-loader": "^0.5.4",
    "node-sass": "^3.7.0",
    "webpack": "^1.13.1"
  },
  "scripts": {
    "build:js": "webpack --optimize-minimize",
    "build:scss": "node-sass --output-style compressed src/styles/app.scss dist/styles/dbp-bundle.min.css --source-map true --source-map-contents sass",
    "build": "npm run build:js && npm run build:scss"
  }
}
